The job you created (<%= link_to(@job, job_url(@job)) %>)  is in pending state.


An admin needs to review the job you created and install the necesary R packages.


You will be notified when the reviewing process is done.
